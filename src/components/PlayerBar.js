import React from "react";

export default function PlayerBar() {
    return (
        <div className="d-flex align-items-center justify-content-center p-3 bg-dark text-white h-100">
            <h5>Trình phát nhạc (Play, Pause, Next, Scrubber...)</h5>
        </div>
    )
}